* Fixes/Updates
    * Changes
        * Rename polar module to hrm
    * Data Entry
        * Reset values after commit.
* New Modules?
    * Location Module
* GUI support
    * Complete rewrite, not being used now.
* Export/Dump data routines
    * sqlite commands
* Create Makefile.PL
    * install target (install in $HOME if not root)
    * test target (load all modules at least)
* graph sub-command
    * use GD to create image files.
* rewrite report sub commands to use SQL syntax
